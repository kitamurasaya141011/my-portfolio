<html>
  <h1>ユーザー一覧</h1>
  <%= link_to '新規登録', new_user_path, class: 'btn btn-primary'%>

  <% if @users.any? %>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>名前</th>
        <th>メールアドレス</th>
        <th>登録日時</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.last_name %> <%= user.first_name %></td> <td><%= link_to '詳細', user_path(user) %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at.strftime("%Y/%m/%d %H:%M") %></td>
          <td><%= button_to '削除', user_path(user), method: :delete, form: { data: { turbo_confirm: "確認" } } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>ユーザーはまだ登録されていません。</p>
<% end %>
</html>
