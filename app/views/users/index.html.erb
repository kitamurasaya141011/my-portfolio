<html>
  <h1>ユーザー一覧</h1>
  <%= link_to '新規登録', new_user_path, class: 'btn btn-primary'%>

  <% if @users.any? %>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>メールアドレス</th>
        <th>登録日時</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.email %></td>
          <td><%= user.created_at.strftime("%Y/%m/%d %H:%M") %></td>
          <td><%= button_to '削除', user_path(user), method: :delete, onclick: "return confirm('本当に削除しますか?');" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>ユーザーはまだ登録されていません。</p>
<% end %>
</html>
